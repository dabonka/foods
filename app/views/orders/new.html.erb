<%= semantic_form_for @order do |f| %>

  <%= f.inputs do %>
    <%= f.input :item_ids, as: :check_boxes, collection: Item.all %>
  <% end %>

  <%= f.actions do %>
      <%= f.action :submit, :as => :button %>    
  <% end %>

<% end %>

