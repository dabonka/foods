<h3>List Users</h3>
<div>
  <table class = "table table-striped">

    <tr>
      <th>ID</th>
      <th>Email</th>
    </tr>

  <% @users = User.all %>
  <% @users.each do |user| %>

    <tr>
      <td><%= user.id %></td>
      <td><%= user.email %></td>
    </tr>

  <% end %>
  </table>
</div>